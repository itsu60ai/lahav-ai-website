---
// HOME, V3.
//
// Section rhythm mapped from the reference package, with LAHAV content
// in each slot:
//   1  hero        full-viewport, oversized stacked statement, guide
//                  character centred, floating product cards, disc CTA
//   2  marquee     continuous type strip, breaks the hero cleanly
//   3  problem     editorial hairline list (the reference's two-column
//                  pattern: heavy heading one side, prose the other)
//   4  core        full-bleed blue contrast band, orbit diagram
//   5  services    pinned storytelling: sticky index, panels swap
//   6  process     numbered editorial rows
//   7  founder     asymmetric portrait + statement
//   8  cta         handled by the footer's oversized closing block
export const prerender = false;

import SiteLayout from '../layouts/SiteLayout.astro';
import Curve from '../components/Curve.astro';
import ContactBlock from '../components/ContactBlock.astro';
import { turnstileSiteKey } from '../lib/turnstile.ts';
import SplitLines from '../components/SplitLines.astro';
import Avatar from '../components/Avatar.astro';
import FloatingCards from '../components/FloatingCards.astro';
import FounderPortrait from '../components/FounderPortrait.astro';
import Viz from '../components/Viz.astro';
import { SERVICES, PROCESS, FOUNDER } from '../lib/site';
import { SERVICE_ICONS } from '../lib/icons';

const SITE_KEY = turnstileSiteKey();

// The four convictions, set as oversized HEBREW words. They used to be
// Latin (PROCESS / BUZZWORD / LESS / CONTROL); that was removed on
// 31.08.2026 along with every other giant English display word.
//
// Statements about how WE work, never about anyone else, and with no
// figures and no claims in them.
const beliefs = [
  {
    kicker: 'קודם העסק, אחר כך הכלי',
    word: 'התהליך',
    body: 'לא נבחר מערכת לפני שנבין איך פנייה מגיעה אליכם, מי נוגע בה ואיפה היא נתקעת.',
  },
  {
    kicker: 'בלי מילים גדולות',
    word: 'בעברית',
    body: 'לא נסביר לכם מה רץ מתחת למכסה המנוע. נגיד מה נבנה ומה זה משנה ביום-יום.',
  },
  {
    kicker: 'לא תמיד צריך לבנות מאפס',
    word: 'רק מה שצריך',
    body: 'אם אפשר לפתור את הצורך עם משהו שכבר קיים, עדיף לדעת את זה לפני שמתחילים לפתח.',
  },
  {
    kicker: 'אתם מחליטים',
    word: 'השליטה אצלכם',
    body: 'המערכת עושה את מה שברור. כל מה שדורש שיקול דעת ממשיך להגיע אליכם.',
  },
];

const pains = [
  { t: 'המידע מפוזר', d: 'חלק בוואטסאפ, חלק במייל, חלק באקסל, וחלק בראש של מישהו. אף אחד לא רואה את התמונה המלאה.' },
  { t: 'אותה עבודה, שוב', d: 'מעתיקים פרטים בין מערכות, ממלאים את אותם טפסים, שולחים את אותן הודעות. כל יום מחדש.' },
  { t: 'פניות שנשכחות', d: 'פנייה נכנסת, אף אחד לא חוזר בזמן, והיא נשארת בלי טיפול.' },
  { t: 'קשה לדעת מה קורה', d: 'שאלה כמו "כמה פניות היו החודש" דורשת לעבור בין כמה מקומות.' },
];

const marquee = ['CRM', 'אוטומציות', 'אתרים', 'אפליקציות', 'תוכן AI', 'אפיון', 'אינטגרציות'];

// What the guide says when a visitor asks it to talk. Timed lines, so
// a real audio track can be dropped in later without touching markup.
// Truth rules apply here exactly as in page copy: no numbers, no
// results, no claims that cannot be stood behind.
const heroScript = [
  { text: 'שלום. אני לַהַב.', hold: 1900 },
  { text: 'רוב העסקים לא צריכים עוד תוכנה.', hold: 2600 },
  { text: 'הם צריכים שהמידע יהיה במקום אחד.', hold: 2800 },
  { text: 'נתחיל מאיך שאתם עובדים היום.', hold: 2600 },
  { text: 'מה שחוזר על עצמו, ירוץ לבד.', hold: 2600 },
  { text: 'וההחלטות נשארות אצלכם.', hold: 2600 },
];

---

<SiteLayout
  title="LAHAV AI, מערכות ואוטומציות שעושות סדר בעסק"
  description="מערכות שעושות סדר במידע העסקי ומטפלות בפעולות שאפשר להגדיר מראש: CRM, אוטומציות, אתרים, אפליקציות ותוכן בסיוע AI."
>
  <!-- ═════════ 1. HERO ═════════ -->
  <section class="hero band band--hero">
    <FloatingCards />

    <!-- Reference composition: the statement owns the top of the screen,
         the character stands beneath it with the round talk trigger over
         its lower body. They sit in sequence, not stacked on top of each
         other, which is what keeps both legible. -->
    <div class="u-container u-container--wide hero__inner">
      <p class="hero__eyebrow t-eyebrow" data-reveal>מערכות AI ואוטומציה לעסקים</p>

      <h1 class="hero__title t-display-1">
        <span class="mo-line" style="--mo-delay:80ms"><span>בסוף מישהו</span></span>
        <span class="mo-line" style="--mo-delay:190ms"><span class="hero__accent">צריך לזכור.</span></span>
      </h1>

      <p class="hero__lead" data-reveal style="--mo-delay:330ms">
        מי דיבר עם הלקוח, מה סוכם ומה עוד צריך לקרות. אנחנו בונים מערכות
        שעושות סדר במידע ומטפלות בפעולות שאפשר להגדיר מראש.
      </p>
    </div>

    <div class="hero__figure">
      <Avatar
        size="hero"
        talkable
        talkLabel="לחצו עליי"
        script={heroScript}
        class="hero__avatar"
      />
    </div>

    <span class="hero__scroll" aria-hidden="true"></span>
  </section>

  <!-- ═════════ 2. MARQUEE ═════════ -->
  <div class="band band--navy mq" aria-hidden="true">
    <div class="mo-marquee" style="--mo-speed:44s;--mo-gap:clamp(2rem,5vw,5rem)">
      <div class="mo-marquee__track">
        {marquee.map((w) => <span class="mq__item">{w}</span>)}
      </div>
    </div>
  </div>

  <!-- ═════════ 3. PROBLEM ═════════ -->
  <section class="u-section band">
    <div class="u-container u-container--wide">
      <div class="prob__head">
        <p class="t-eyebrow" data-reveal>נשמע מוכר?</p>
        <SplitLines
          class="t-display-2 prob__title"
          lines={['ככה זה נראה', 'כשהעבודה מתפזרת.']}
          step={100}
        />
      </div>

      <ul class="prob__list" data-stagger style="--mo-step:110ms">
        {pains.map((p, i) => (
          <li class="prob__row" data-reveal>
            <span class="prob__n tabular">{String(i + 1).padStart(2, '0')}</span>
            <h3 class="prob__t t-display-3">{p.t}</h3>
            <p class="prob__d">{p.d}</p>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- ═════════ 4. THE CORE ═════════ -->
  <section class="core band band--blue">
    <Curve tone="blue" shape="wave" />

    <div class="u-container u-container--wide core__head">
      <p class="t-eyebrow core__k" data-reveal>הרעיון</p>
      <h2 class="core__h">
        <span class="mo-line" style="--mo-delay:80ms"><span>פותחים מסך אחד</span></span>
        <span class="mo-line" style="--mo-delay:190ms"><span class="core__h-2">ורואים איפה הכל עומד.</span></span>
      </h2>
      <p class="core__sub" data-reveal style="--mo-delay:300ms">
        הפניות, הלקוחות והמשימות נכנסים לאותו מקום. משם כבר אפשר לענות על
        שאלות בלי לחפש.
      </p>
    </div>

    <!-- The core itself. One glowing centre, four capabilities orbiting
         it, drawn rather than photographed so nothing here implies a
         product screenshot or a result we cannot stand behind. -->
    <div class="core__stage" data-scale-in>
      <span class="core__orb" aria-hidden="true"></span>
      <span class="core__ring core__ring--1" aria-hidden="true"></span>
      <span class="core__ring core__ring--2" aria-hidden="true"></span>
      <span class="core__ring core__ring--3" aria-hidden="true"></span>
      <ul class="core__sats">
        <li class="core__sat core__sat--a"><span>פניות</span></li>
        <li class="core__sat core__sat--b"><span>לקוחות</span></li>
        <li class="core__sat core__sat--c"><span>משימות</span></li>
        <li class="core__sat core__sat--d"><span>מעקב</span></li>
      </ul>
    </div>

    <div class="u-container u-container--wide core__foot">
      <p class="core__lead" data-reveal>
        מישהו משאיר פרטים באתר. הפנייה נרשמת לבד, מקבלת מענה ראשוני, נכנסת
        למעקב, ומזכירה לכם לחזור אליה בזמן. אף אחד לא העתיק שורה.
      </p>
      <div class="core__viz" data-clip style="--mo-delay:160ms">
        <Viz kind="crm" />
      </div>
    </div>
  </section>

  <!-- ═════════ 5. WHAT WE BUILD ═════════ -->
  <!-- Cards on a rail, the way the reference presents its offering:
       one card per service, each self-contained, swipeable on a phone
       and three-up on a desktop. -->
  <section class="svc u-section band">
    <Curve tone="surface" shape="slope" />

    <div class="u-container u-container--wide svc__head">
      <p class="t-eyebrow" data-reveal>מה אנחנו בונים</p>
      <h2 class="svc__h t-display-2" data-reveal style="--mo-delay:80ms">מתחילים במה<br />שהכי מציק.</h2>
      <p class="svc__lead t-lead" data-reveal style="--mo-delay:160ms">
        בוחרים את הנקודה שהכי מפריעה לעבודה ומתחילים ממנה.
      </p>
    </div>

    <ul class="svc__rail" data-svc-rail>
      {SERVICES.map((s, i) => (
        <li class="svc__card" data-reveal style={`--mo-delay:${i * 70}ms`}>
          <span class="svc__icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"
                 stroke-linecap="round" stroke-linejoin="round" set:html={SERVICE_ICONS[s.slug]} />
          </span>
          <span class="svc__n tabular">{String(i + 1).padStart(2, '0')}</span>
          <h3 class="svc__name">{s.name}</h3>
          <p class="svc__card-lead">{s.lead}</p>
          <ul class="svc__points">
            {s.points.map((pt) => <li>{pt}</li>)}
          </ul>
          <a class="svc__go" href={`/services/${s.slug}/`}>
            לראות איך זה עובד <span class="arrow-inline" aria-hidden="true">←</span>
          </a>
        </li>
      ))}
    </ul>
  </section>

  <!-- ═════════ 6. PROCESS ═════════ -->
  <section class="proc u-section band band--warm">
    <Curve tone="warm" shape="wave" flip />
    <div class="u-container u-container--wide">
      <div class="proc__head">
        <p class="t-eyebrow" data-reveal>איך זה מתנהל</p>
        <SplitLines class="t-display-2" lines={['שישה שלבים.', 'יודעים מה קורה בכל אחד.']} step={100} />
      </div>

      <ol class="proc__list" data-stagger style="--mo-step:90ms">
        {PROCESS.map((p) => (
          <li class="proc__row" data-reveal>
            <span class="proc__n tabular">{p.n}</span>
            <h3 class="proc__t t-h2">{p.label}</h3>
            <p class="proc__d">{p.what}</p>
          </li>
        ))}
      </ol>
    </div>
  </section>

  <!-- ═════════ 7. WHAT WE BELIEVE ═════════ -->
  <!-- The reference's About block: a grid of oversized Latin words, each
       with a small Hebrew kicker above and Hebrew prose below. One word
       is struck through, which is the whole point of it. -->
  <section class="blf u-section band band--soft">
    <Curve tone="soft" shape="wave" />
    <div class="u-container u-container--wide">
      <div class="blf__head">
        <p class="t-eyebrow" data-reveal>אודות</p>
        <h2 class="blf__h t-display-3" data-reveal style="--mo-delay:80ms">
          איך אנחנו עובדים
        </h2>
      </div>

      <ul class="blf__grid">
        {beliefs.map((b, i) => (
          <li class="blf__item" data-reveal style={`--mo-delay:${i * 90}ms`}>
            <p class="blf__kicker">{b.kicker}</p>
            <p class="blf__word">{b.word}</p>
            <p class="blf__body">{b.body}</p>
          </li>
        ))}
      </ul>

      <a class="link-arrow blf__more" href="/about/" data-reveal>
        הסיפור המלא <span class="arrow-inline" aria-hidden="true">←</span>
      </a>
    </div>
  </section>

  <!-- ═════════ 8. FOUNDER ═════════ -->
  <section class="u-section band fnd">
    <Curve tone="surface" shape="slope" flip />
    <div class="u-container u-container--wide fnd__grid">
      <div class="fnd__art" data-clip>
        <FounderPortrait scale="home" sizes="(max-width: 1024px) 70vw, 30rem" />
      </div>

      <div class="fnd__copy">
        <p class="t-eyebrow" data-reveal>מי עומד מאחורי זה</p>
        <SplitLines class="t-display-3 fnd__name" lines={[FOUNDER.name]} />
        <p class="fnd__role" data-reveal style="--mo-delay:120ms">{FOUNDER.role}</p>
        <p class="fnd__p" data-reveal style="--mo-delay:180ms">
          הקמתי את LAHAV AI כדי להתחיל מהעסק ולא מהטכנולוגיה. המידע מתפזר
          בין מערכות, העבודה הידנית מצטברת, וקשה לדעת מה באמת קורה.
        </p>
        <p class="fnd__p" data-reveal style="--mo-delay:240ms">
          לכן קודם נבין איפה העבודה נתקעת ומה גוזל לכם את הזמן. רק אחר כך
          נבחר כלים.
        </p>
        <a class="link-arrow" href="/about/" data-reveal style="--mo-delay:300ms">
          עוד עלינו <span class="arrow-inline" aria-hidden="true">←</span>
        </a>
      </div>
    </div>
  </section>

  <!-- ═════════ 9. CONTACT ═════════ -->
  <ContactBlock siteKey={SITE_KEY} />
</SiteLayout>

<style>
  /* ══════ hero ══════
     Sequence, not overlay: eyebrow, statement, then the character
     directly beneath it with the talk trigger over its lower body. The
     whole act is sized to land inside one screen. */
  .hero {
    position: relative;
    min-height: 100svh;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding-block: clamp(5.5rem, 9svh, 7.5rem) 0;
    overflow: clip;
  }

  .hero__inner { position: relative; z-index: 3; text-align: center; width: 100%; }
  .hero__eyebrow { margin-bottom: clamp(0.5rem, 1.2vw, 1rem); }
  /* Height-aware, not just width-aware. On a wide-but-short screen
     (1920x988, the reference recording's own viewport) a pure vw size
     eats the room the character needs, and the character is the point of
     this hero. The svh term is what keeps the whole act on one screen. */
  .hero__title {
    margin-inline: auto;
    font-size: min(8.4vw, 13.5svh, 11rem);
  }
  .hero__lead {
    max-width: 44ch;
    margin-inline: auto;
    margin-top: clamp(0.75rem, 1.6vw, 1.25rem);
    font-size: var(--text-lead);
    line-height: 1.55;
    color: var(--color-ink-2);
  }
  .hero__accent {
    background: linear-gradient(100deg, #1d4ed8 0%, #2997ff 100%);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  /* the character claims whatever height is left below the statement */
  .hero__figure {
    position: relative;
    z-index: 2;
    flex: 1 1 auto;
    min-height: 0;
    display: grid;
    /* bottom-aligned: the character stands on the fold the way the
       reference presenter stands on its floor, rather than floating in
       the middle of the leftover space */
    place-items: end center;
    width: 100%;
    padding-bottom: clamp(1.5rem, 3.5svh, 3rem);
    margin-top: clamp(-2rem, -2vw, -0.5rem);
  }
  .hero__figure :global(.av) { gap: 0; height: 100%; justify-content: center; }
  /* height-driven so the figure can never push the hero past one screen */
  .hero__figure :global(.av--hero) { --av-w: auto; }
  .hero__figure :global(.av__figure) { height: 100%; display: grid; place-items: center; }
  .hero__figure :global(.av__svg) { height: 100%; width: auto; max-height: min(46svh, 29vw); }

  /* the round trigger overlaps the character's lower body */
  /* The trigger overlaps the character's hip, the way the reference's
     round CTA overlaps its presenter. It is deliberately smaller than the
     standard disc: at full size it would cover the whole figure, since a
     drawn character is narrower than a rendered human one. */
  .hero__figure :global(.av__talk) {
    position: absolute;
    bottom: clamp(4.5rem, 15.5svh, 9.5rem);
    inset-inline-start: 50%;
    transform: translateX(50%);
    margin: 0;
    width: clamp(5.5rem, 6vw, 6.75rem);
    font-size: var(--text-small);
  }

  .hero__scroll {
    position: absolute;
    bottom: 1.25rem;
    inset-inline-start: 50%;
    width: 1px;
    height: 3rem;
    background: linear-gradient(180deg, transparent, var(--color-ink-3));
    opacity: 0.5;
    animation: hero-scroll 2.4s var(--ease-inout) infinite;
    z-index: 4;
  }
  @keyframes hero-scroll {
    0%, 100% { transform: scaleY(0.4); transform-origin: top; opacity: 0.2; }
    50%      { transform: scaleY(1); transform-origin: top; opacity: 0.6; }
  }

  /* ══════ marquee ══════ */
  .mq { padding-block: clamp(1.25rem, 2.5vw, 2rem); }
  .mq__item {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 3.4vw, 3rem);
    font-weight: 900;
    letter-spacing: -0.02em;
    color: rgba(255, 255, 255, 0.9);
    white-space: nowrap;
  }
  .mq__item::after {
    content: '—';
    margin-inline-start: clamp(2rem, 5vw, 5rem);
    color: var(--color-electric);
  }

  /* ══════ problem ══════ */
  .prob__head { margin-bottom: clamp(3rem, 6vw, 5rem); }
  .prob__title { margin-top: 1rem; }
  .prob__list { list-style: none; margin: 0; padding: 0; }
  .prob__row {
    display: grid;
    grid-template-columns: 4rem minmax(0, 1.1fr) minmax(0, 1fr);
    gap: clamp(1rem, 3vw, 3rem);
    align-items: baseline;
    padding-block: clamp(1.75rem, 3vw, 2.75rem);
    border-top: 1px solid var(--color-line);
  }
  .prob__row:last-child { border-bottom: 1px solid var(--color-line); }
  .prob__n { font-family: var(--font-display); font-weight: 700; color: var(--color-electric); font-size: var(--text-small); }
  .prob__d { color: var(--color-ink-2); max-width: 46ch; }

  /* ====== the core ====== */
  .core {
    position: relative;
    padding-block: var(--space-section);
    overflow: visible;
    text-align: center;
  }
  .core__head { position: relative; z-index: 2; }
  .core__k { color: rgba(255, 255, 255, 0.72); }
  .core__h {
    display: flex;
    flex-direction: column;
    align-items: center;
    font-family: var(--font-display);
    font-size: var(--text-display-3);
    font-weight: 900;
    line-height: 0.98;
    letter-spacing: -0.035em;
    margin-top: 0.75rem;
    color: #fff;
  }
  .core__h-2 { color: rgba(255, 255, 255, 0.62); }
  .core__sub {
    margin: 1rem auto 0;
    max-width: 46ch;
    font-size: var(--text-lead);
    line-height: 1.55;
    color: rgba(255, 255, 255, 0.88);
  }

  /* The core: a lit centre with capabilities in orbit. Pure CSS, so it
     scales to any viewport and costs nothing to load. */
  .core__stage {
    position: relative;
    width: min(88vw, 34rem);
    aspect-ratio: 1;
    margin: clamp(2rem, 5vw, 4rem) auto clamp(1.5rem, 4vw, 3rem);
    display: grid;
    place-items: center;
  }
  .core__orb {
    width: 42%;
    aspect-ratio: 1;
    border-radius: 50%;
    background: radial-gradient(circle at 38% 32%, #fff 0%, #cfe6ff 22%, #7fb6ff 48%, #2f6bd6 78%, #1c3f92 100%);
    box-shadow: 0 0 90px rgba(150, 205, 255, 0.55), inset -10px -14px 40px rgba(9, 30, 80, 0.5);
    animation: core-breathe 7s var(--ease-inout) infinite;
  }
  @keyframes core-breathe {
    0%, 100% { transform: scale(1) }
    50%      { transform: scale(1.045) }
  }
  /* The rings do NOT rotate. Rotating a square element with a 50% radius
     swings its bounding box out to the diagonal, which pushed the whole
     document ~95px wider than a phone viewport. It is also invisible
     motion: a plain circle looks identical at every angle. They breathe
     instead, which is both visible and contained. */
  .core__ring {
    position: absolute;
    border: 1px solid rgba(255, 255, 255, 0.28);
    border-radius: 50%;
    animation: core-ring 9s var(--ease-inout) infinite;
  }
  .core__ring--1 { inset: 22%; }
  .core__ring--2 { inset: 8%;  border-color: rgba(255, 255, 255, 0.2);  animation-delay: -3s; }
  .core__ring--3 { inset: 0;   border-color: rgba(255, 255, 255, 0.14); animation-delay: -6s; }
  @keyframes core-ring {
    0%, 100% { opacity: 1;    transform: scale(1) }
    50%      { opacity: 0.45; transform: scale(1.015) }
  }

  .core__sats { list-style: none; margin: 0; padding: 0; position: absolute; inset: 0; }
  .core__sat {
    position: absolute;
    display: grid;
    place-items: center;
    padding: 0.5rem 1rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.16);
    border: 1px solid rgba(255, 255, 255, 0.38);
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    color: #fff;
    font-family: var(--font-display);
    font-size: clamp(0.75rem, 1.3vw, 1rem);
    font-weight: 800;
    white-space: nowrap;
    animation: core-float 6s var(--ease-inout) infinite;
  }
  .core__sat--a { inset-block-start: 2%;  inset-inline-start: 50%; transform: translateX(50%); }
  .core__sat--b { inset-block-start: 46%; inset-inline-start: -4%; animation-delay: -1.5s; }
  .core__sat--c { inset-block-start: 46%; inset-inline-end: -4%;   animation-delay: -3s; }
  .core__sat--d { inset-block-end: 2%;    inset-inline-start: 50%; transform: translateX(50%); animation-delay: -4.5s; }
  @keyframes core-float {
    0%, 100% { translate: 0 0 }
    50%      { translate: 0 -8px }
  }

  .core__foot {
    position: relative;
    z-index: 2;
    display: grid;
    grid-template-columns: minmax(0, 0.8fr) minmax(0, 1.2fr);
    gap: clamp(1.5rem, 4vw, 3.5rem);
    align-items: center;
    text-align: start;
  }
  .core__lead { color: rgba(255, 255, 255, 0.9); font-size: var(--text-lead); line-height: 1.6; }
  .core__viz { border-radius: var(--radius-xl); overflow: hidden; }

  /* ====== what we build ====== */
  .svc { position: relative; overflow: visible; }
  .svc__head { max-width: 46rem; margin-bottom: clamp(2rem, 4vw, 3.5rem); }
  .svc__h { margin-block: 0.75rem 1.25rem; }
  .svc__lead { max-width: 48ch; }

  /* A rail, not a grid: five cards never divide evenly into three
     columns, and a swipeable rail is the honest mobile-first answer. */
  .svc__rail {
    list-style: none;
    margin: 0;
    padding: 0 max(var(--gutter, 1.25rem), calc((100vw - var(--container-wide)) / 2)) 1rem;
    display: grid;
    grid-auto-flow: column;
    grid-auto-columns: minmax(19rem, 1fr);
    gap: clamp(1rem, 2vw, 1.5rem);
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    overscroll-behavior-x: contain;
    scrollbar-width: thin;
  }
  .svc__card {
    scroll-snap-align: start;
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
    padding: clamp(1.25rem, 2.4vw, 1.9rem);
    border-radius: var(--radius-xl);
    background: linear-gradient(165deg, #2456d8 0%, #1d4ed8 42%, #16308c 100%);
    color: #fff;
    box-shadow: 0 20px 50px rgba(16, 38, 94, 0.22);
    transition: transform var(--dur) var(--ease-out), box-shadow var(--dur) var(--ease-out);
  }
  .svc__card:hover { transform: translateY(-6px); box-shadow: 0 28px 66px rgba(16, 38, 94, 0.3); }
  .svc__icon {
    display: grid; place-items: center;
    width: 2.75rem; height: 2.75rem;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.16);
    border: 1px solid rgba(255, 255, 255, 0.34);
    color: #fff;
  }
  .svc__icon :global(svg) { width: 1.3rem; height: 1.3rem; }
  .svc__n { font-family: var(--font-display); font-size: var(--text-micro); font-weight: 700; color: rgba(255,255,255,0.6); }
  .svc__name {
    font-family: var(--font-display);
    font-size: clamp(1.375rem, 2.1vw, 1.875rem);
    font-weight: 800;
    line-height: 1.12;
    letter-spacing: -0.02em;
    color: #fff;
  }
  .svc__card-lead { color: rgba(255, 255, 255, 0.9); font-size: var(--text-small); line-height: 1.6; }
  .svc__points { list-style: none; margin: 0.35rem 0 0; padding: 0; display: flex; flex-direction: column; gap: 0.45rem; }
  .svc__points li {
    position: relative;
    padding-inline-start: 1.15rem;
    font-size: var(--text-micro);
    line-height: 1.55;
    color: rgba(255, 255, 255, 0.82);
  }
  .svc__points li::before {
    content: '';
    position: absolute;
    inset-inline-start: 0;
    top: 0.62em;
    width: 0.7rem; height: 2px;
    background: rgba(255, 255, 255, 0.6);
  }
  .svc__go {
    margin-top: auto;
    padding-top: 1.1rem;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    color: #fff;
    font-weight: 700;
    font-size: var(--text-small);
    text-decoration: none;
  }
  .svc__go .arrow-inline { transition: transform var(--dur) var(--ease-out); }
  .svc__card:hover .svc__go .arrow-inline { transform: translateX(-5px); }

  /* ====== beliefs ====== */
  .blf { position: relative; overflow: visible; }
  .blf__head { margin-bottom: clamp(2rem, 4vw, 3.5rem); }
  .blf__h { margin-top: 0.75rem; }
  .blf__grid {
    list-style: none; margin: 0; padding: 0;
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: clamp(2rem, 5vw, 4.5rem) clamp(2rem, 6vw, 6rem);
  }
  .blf__item { padding-top: 1.25rem; border-top: 1px solid var(--color-line-strong); }
  .blf__kicker { font-size: var(--text-small); font-weight: 700; color: var(--color-ink-3); }
  .blf__word {
    font-family: var(--font-display);
    font-size: clamp(1.875rem, 4.4vw, 3.75rem);
    font-weight: 900;
    line-height: 1.02;
    letter-spacing: -0.035em;
    color: var(--color-royal);
    margin-block: 0.35rem 0.9rem;
  }
  .blf__body { color: var(--color-ink-2); line-height: 1.7; max-width: 44ch; }
  .blf__more { margin-top: clamp(2rem, 4vw, 3rem); }

  .fnd { position: relative; overflow: visible; }

  /* ══════ process ══════ */
  .proc__head { margin-bottom: clamp(3rem, 6vw, 5rem); }
  .proc__list { list-style: none; margin: 0; padding: 0; }
  .proc__row {
    display: grid;
    grid-template-columns: 4rem minmax(0, 0.9fr) minmax(0, 1.2fr);
    gap: clamp(1rem, 3vw, 3rem);
    align-items: baseline;
    padding-block: clamp(1.5rem, 2.6vw, 2.25rem);
    border-top: 1px solid var(--color-line-strong);
  }
  .proc__row:last-child { border-bottom: 1px solid var(--color-line-strong); }
  .proc__n { font-family: var(--font-display); font-weight: 800; color: var(--color-electric); }
  .proc__d { color: var(--color-ink-2); max-width: 52ch; }

  /* ══════ founder ══════ */
  .fnd__grid {
    display: grid;
    grid-template-columns: minmax(0, 0.85fr) minmax(0, 1.15fr);
    gap: clamp(2rem, 5vw, 5rem);
    align-items: center;
  }
  .fnd__art { border-radius: var(--radius-xl); overflow: hidden; }
  .fnd__name { margin-top: 0.75rem; }
  .fnd__role {
    font-size: var(--text-small); font-weight: 700; color: var(--color-royal);
    direction: ltr; text-align: start; margin-block: 0.5rem 1.5rem;
  }
  .fnd__p { color: var(--color-ink-2); margin-bottom: 1rem; max-width: 54ch; }

  /* ══════ responsive ══════ */
  @media (max-width: 1024px) {
    .core__inner, .fnd__grid { grid-template-columns: 1fr; }
    .core__foot { grid-template-columns: 1fr; }
    .fnd__art { max-width: 26rem; }
  }
  @media (max-width: 760px) {
    .prob__row, .proc__row { grid-template-columns: 3rem 1fr; }
    .prob__d, .proc__d { grid-column: 2; }
    .blf__grid { grid-template-columns: 1fr; }
    .svc__rail { grid-auto-columns: minmax(15rem, 82vw); }
    /* the orbiting labels hang 4% outside the stage, which on a narrow
       screen is 4% outside the viewport */
    .core__stage { width: min(78vw, 34rem); }
    .core__sat { padding: 0.4rem 0.7rem; }
    .core__sat--b { inset-inline-start: 0; }
    .core__sat--c { inset-inline-end: 0; }

    /* Mobile keeps the same sequence, just tighter, and the character
       gets a smaller share so the statement still leads. */
    .hero { padding-top: 6rem; }
    .hero__title { font-size: clamp(1.85rem, 8.8vw, 2.75rem); }
    .hero__figure :global(.av__svg) { max-height: 40svh; }
    /* the trigger keeps overlapping the character on mobile too, the
       way the reference's round CTA sits over its presenter's thighs */
    .hero__figure :global(.av__talk) { bottom: 19svh; width: 5.75rem; font-size: var(--text-micro); }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero__scroll { animation: none; }
  }
</style>
